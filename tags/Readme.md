git tah 1.0.0 # tag a commit

git push --tags # push tag to remote

git checkout 1.0.0 # checkout codebase at tag

git tag -d 1.0.0 # delete local tag

git push --delete origin tagname # delete remote tag